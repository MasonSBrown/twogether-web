rules_version = '2';
service cloud.firestore {
  match /databases/{database}/documents {
    // Allow public write access to the waitlist collection
    match /waitlist/{document=**} {
      allow create: if true;
      allow read, update, delete: if false;
    }
  }
}
